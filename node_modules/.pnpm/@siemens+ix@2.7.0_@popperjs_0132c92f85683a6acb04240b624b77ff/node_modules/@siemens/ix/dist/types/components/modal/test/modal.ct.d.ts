import { dismissModal, showModal } from './../../utils/modal';
declare global {
    interface Window {
        dismissModal: typeof dismissModal;
        showModal: typeof showModal;
        __counter: number;
    }
}
