export declare function hasAnyText({ invalidText, warningText, infoText, validText, helperText, }: {
    invalidText?: string;
    warningText?: string;
    infoText?: string;
    validText?: string;
    helperText?: string;
}): string | undefined;
export declare function renderHelperText({ isInvalid, invalidText, isWarning, warningText, isInfo, infoText, isValid, validText, helperText, }: {
    isInvalid: boolean;
    invalidText?: string;
    isWarning: boolean;
    warningText?: string;
    isInfo: boolean;
    infoText?: string;
    isValid: boolean;
    validText?: string;
    helperText?: string;
}): any;
